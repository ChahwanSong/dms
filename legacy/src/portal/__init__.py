"""DMS Portal — operator/user-facing web UI.

A FastAPI BFF (``portal.backend``) plus a Vite SPA (``src/portal/frontend``). The
portal is a pure client of the DMS HTTP API; it never imports the DMS backend.
"""
