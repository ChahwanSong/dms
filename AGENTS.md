# Instruction

이 파일은 DMS 구현 및 검증 시 코드 에이전트가 참고해야 할 가이드 문서이다. 

## 구현 가이드 

DMS 디렉토리는 /home/workspace/mason/dms 이다. 

다음은 구현 가이드이다:
- DMS 구현은 설계 철학 및 원칙 하에 일관성 있게 구현한다.
- DMS 는 기능 별로 모듈화하여 코드를 구현하며, 실무적으로 적합한 코드 디자인 패턴을 구현한다.
- DMS 는 실제 클러스터 환경에서 배포를 가정하고 구현을 하며, 테스트베드 는 기능 검수용이다. 테스트베드에 의해 구현이 제한되어서는 안된다.
- 매번 MS 구현시 모든 기능은 코드 리뷰 및 개선작업, 그리고 테스트베드에서의 기능 검증이 되어야 한다. 기능 검증시 테스트베드 환경상 불가능한 경우, 검증이 안된 사항을 구현 후 반드시 명시해야 한다.

DMS 구현은 incremental 하게 진행될 것이나, 코드 구현  추후 refactoring 을 진행하지 않아도 될 정도로 완성도있게 진행한다. 즉, 덕지덕지 붙거나 덮어쓰는 파일이 여러개 생기는 경우를 피한다.

## 테스트베드

테스트베드 디렉토리는 /home/mason/workspace/testbed 이다. 

다음은 테스트베드 사용 가이드이다:
- 테스트베드는 두개의 쿠버네티스 클러스터로 구성되어 있다: c1, c2
- c1 은 DMS 가 동작하는 클러스터 이다.
- c2 은 쿠버네티스 멀티 클러스터 환경에서 DMS 외에 다른 클러스터이다. 예를 들면, A100 GPU cluster 이고 여기에 전용 스토리지가 있을 수 있다.
- 테스트베드 디렉토리의 문서에 설치되있는 패키지 및 시스템이 있다. 설치가 필요한 경우 설치하고 문서를 남긴다. 
- 파일시스템의 계정 권한 기능 검수 시 linux local 기능을 쓰지 말고 테스트베드의 LDAP 을 사용한다.
