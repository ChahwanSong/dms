# DMS Agent Instructions

이 파일은 Codex가 DMS 프로젝트를 구현, 수정, 테스트, 문서화할 때 따라야 하는 작업 지침이다.

DMS 작업 전 이 파일을 먼저 읽고, 관련 문서들을 읽는다.
DMS구현 후 검증은 테스트베드에서 진행하며, 테스트베드 메타데이터를 확인한 후 진행한다.
DMS 구현 시 테스트베드에 국한되어서 구현하는 것이 아니라, 실제 데이터센터 환경에서도 적용 가능하도록 구현한다. 
테스트베드에서 검증하면서 필요한 패키지 또는 시스템 등에 대해서는 설치 후 관련 정보를 테스트베드 구성현황 문서 디렉토리에 추가하거나 업데이트한다.


## Project Goal

DMS는 실제 데이터센터의 다양한 Storage Backends (파일시스템, CSI) 를 가지고 있는 Kubernetes 멀티-클러스터 환경에서 설치와 운영이 가능하도록 구현한다.

테스트베드는 로컬 데스크탑 위 Vagrant 기반 VM 환경에서 Kubernetes 멀티 클러스터 형태로 구성되어 있으며, 
로컬 데스크탑의 리소스가 제약적이므로, 이 환경에서 최대한 효율적으로 가능한 많은 DMS 기능을 검증하도록 한다.


## Required Reading Before Work

작업을 시작하기 전에 목적에 따라 아래 파일을 확인한다.

- (**가장 중요한 문서**) 전체 설계와 아키텍처를 이해해야 하는 경우:
  - `/home/mason/workspace/dms/docs/dms-design.md` 문서 전체를 읽는다.
- (**현재 테스트베드 구성 현황**) 테스트베드의 현재 상태와 구성 요소를 이해해야 하는 경우:
  - `/home/mason/workspace/testbed` 디렉토리의 모든 문서와 파일을 읽는다.
  - 테스트베드에서 검증시 필요하지만 구축되어 있지 않은 시스템 (예: 스토리지)는 필요하다면 설치 및 셋업하고 문서를 추가한다.
  - 시스템/패키지/컴포넌트 등이 문서와 실제 상태와 일치하지 않으면 하면 문서를 추가/업데이트 한다.
- (**구체적인 구현 작업**) 특정 컴포넌트나 기능을 구현하기 전에:
  - `/home/mason/workspace/dms/docs/dms-design.md` 문서에서 해당 컴포넌트나 기능과 관련된 섹션을 꼼꼼히 읽는다.
